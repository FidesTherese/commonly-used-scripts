# commonly-used-scripts
Backup of commonly used shell scripts.

## Purpose
This repository is a backup of shell scripts that I am always using in working environment. All these shell scripts are developed by me and run on Zsh terminal.

## Usage
To activate any of the scripts uploaded here, edit the `~/.zshrc` and add `source /PATH/SCRIPT.zsh`.
